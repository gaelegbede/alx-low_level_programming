#include <stdio.h>
#include <stdlib.h>
#include <time.h>
#include <ctype.h>
/**
 * main = Entry point
 */
int main(void)
{
	int w;
	for (w = 0; w < 10; w++)
{
	printf("%d", w);
}
{
	putchar ('\n');
}
return (0);
}
