#include <stdio.h>
#include <stdlib.h>
#include <time.h>
#include <ctype.h>
/**
 * main - Entry point
 *Return: Always (0) Success
 */
int main(void)
{
	int w;
/*for printing lowercase alphabets*/
	for (w = 0; w < 10; w++)

{
	printf("%d", w);
}
{
	putchar ('\n');
}
return (0);
}
